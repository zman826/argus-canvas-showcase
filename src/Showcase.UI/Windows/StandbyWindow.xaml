<Window x:Class="Showcase.UI.Windows.StandbyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CIMA Showcase"
        WindowStyle="None"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Background="White"
        FontFamily="{StaticResource PrimaryFontFamily}">
    <Grid>
        <!-- 円形フローティング演出のキャンバス（最背面）。
             StandbyWindow.xaml.cs の InitializeAnimation() で
             Constants.CircleCount 個の円を生成・アニメーションさせる。 -->
        <Canvas x:Name="AnimationCanvas"
                Panel.ZIndex="0"
                Background="White" />

        <!-- 上部中央：テキストロゴ -->
        <TextBlock Text="CIMA"
                   Panel.ZIndex="10"
                   FontSize="48"
                   FontWeight="Bold"
                   Foreground="{StaticResource CimaBrandBrush}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,80,0,0" />

        <!-- 下部中央：誘導メッセージ -->
        <TextBlock Text="🎙️ 話しかけてください　/　👆 タッチしてください"
                   Panel.ZIndex="10"
                   FontSize="28"
                   Foreground="#333333"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,100" />

        <!-- 全面透明ボタン：クリック/タッチで Home へ -->
        <Button x:Name="TouchButton"
                Background="Transparent"
                BorderThickness="0"
                Click="OnTouch"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
